#include<iostream>
#include <vector>
#include<numeric>
using namespace std;

vector<int> arr(const int n = 0) {
	vector<int> result(n);
	iota(result.begin(), result.end(), 0);
	return result;
}
int main() {
	int n = 0;
	return 0;
}